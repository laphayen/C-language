
#include <stdio.h>

void main()
{
	int num = 11618;

	// if - 식
	if (num == 11618)
		printf("num은 11618입니다.");
	
	// if가 아닐 경우 실행
	else if (num > 11618)
		printf("num은 11618보다 큽니다.");
	
	// if와 else가 아닐 경우 실행
	else
		printf("num은 11618이 아닙니다.");
}
